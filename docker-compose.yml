services:
  sql:
    image: mcr.microsoft.com/mssql/server:2022-latest
    container_name: PersonalTrainer-sql
    env_file:
      - .env
    environment:
      ACCEPT_EULA: "Y"
    ports:
      - "1433:1433"
    volumes:
      - sqldata:/var/opt/mssql

volumes:
  sqldata:
